/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['upload.wikimedia.org', 'u', 'raw.githubusercontent.com', 'global.discourse-cdn.com', 'static-00.iconduck.com'],
    },
}

module.exports = nextConfig
